# pixi-managed conda environment (pixi.lock IS tracked)
/.pixi/

# Repo-local build output (Spack install tree, caches, env view, logs, etc.)
/working_dir/

# Spack directory-environment runtime state (view + generated lockfile), one set
# per variant. The variant manifests (spack-env/<variant>/spack.yaml) and the
# shared spack-env/common.yaml ARE tracked; the generated .spack-env/ + spack.lock
# are regenerated by `build` (concretize -f), so they are not.
/spack-env/*/.spack-env/
/spack-env/*/spack.lock

# Logs / batch output
*.log
*.out
*.err
/.submodule-add.log

# Python / OS cruft
__pycache__/
*.pyc
*~
*.swp
*.swo
.DS_Store
